name: Basic workflow

on:
  push:
    branches:
      - main

jobs:
  basic-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello, world!"
      - name: Joppe
        run: echo "Ciao da Joppe"
